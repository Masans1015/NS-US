# AIアプリ内製プログラム

NouScale／1名＋AI／2026-09-01 〜 2026-11-29（13週・稼働 **174h**）。
社内利用を先行し、可搬性を担保したうえで外販候補を選抜する。

**着手対象は確定5本（A15・A03・A04・A11・A09）＋条件付き1本（A13）。A18・A14 は次期送り。**
週割り・工数・ゲート判定日は `docs/plan.md`（v1.1）が唯一の基礎。確定した設計判断は `docs/decisions.md`。

---

## 最重要の設計原則

**core（判断ロジック）に会社固有を一切書かない。**

会社固有はすべて `org/org-profile.{org_id}.json` に外出しする。
この分離が崩れた時点で、社内で動くものはできても外販版は作り直しになる。

| 層 | 置くもの | 置いてはいけないもの |
|---|---|---|
| `core/` | 分解の型、検査ロジック、欠落検出、判定アルゴリズム、汎用の検品項目 | 会社名、規程条文、工数実績、章立て、色・フォント |
| `org/` | データ区分、禁止入力、承認済みツール、工数実績、標準条項、章立て、トーン | 判断のアルゴリズム |
| `render/` | フォント、色、レイアウト、生成に使うスキル名 | 内容に関わる判断 |

何を書くかは core、どう見せるかは render、誰の話かは org。

### 分離の検査（コミット前に必ず実行）

```bash
./scripts/check-separation.sh
```

`core/` 配下に会社固有語が1件でもヒットしたら失敗。
機能を足すのではなく、分離をやり直すこと。

---

## ディレクトリ

```
core/       {app_id}.system.md, {app_id}.rubric.json
org/        org-profile.{org_id}.json
render/     generic/, rcg/
eval/       {app_id}.evalset.json
apps/       {app_id}/index.html  ← 単体HTML。ブラウザ内完結
telemetry/  {app_id}.{YYYY-MM}.jsonl
schemas/    このプロジェクトの定義（変更時は必ず schema_version を上げる）
docs/       要件定義・ゲート定義
scripts/    検査スクリプト
```

---

## 実装の制約

- **全アプリ TierA（単体HTML・ブラウザ内完結）。** 情シス承認が未確認のため、Supabase / Vercel / 外部DBは使わない
- **localStorage / sessionStorage は使わない。** 状態はJS変数で持ち、telemetryは都度ダウンロードで回収する
- **org-profile はアプリに埋め込まず、ファイル読込にする。** 埋め込んだ瞬間に差し替え不能になる
- **A03 / A04 は LLM を使わずに本体機能を成立させる。** 利用者が全社員のため、各人にAPIキーを持たせられない。
  区分判定は `policy.data_classification` の判定木と `prohibited_inputs[].detect_patterns` の正規表現で行う。
  LLM は匿名化案の文面生成だけに任意で使い、**キー未入力でも本体機能が完全に動く**こと（ADR-005）
- **LLM層（A15 / A11 / A09）は Anthropic API のみ。** 呼び出し規約は ADR-006 で固定済み。共通シェルに1箇所だけ実装する

  ```js
  const MODEL = { PRIMARY: 'claude-opus-5', ALT: 'claude-sonnet-5' };  // ALT は G5 の代替モデル
  ```

  `thinking: { type: 'adaptive' }`（`budget_tokens` は400エラー）／出力は `output_config.format` の構造化出力（prefill は400エラー）／
  **`stop_reason === 'refusal'` を必ず確認する**（`content` を先に読むと空結果を「違反なし」と誤表示する）
- **A18 は合成データのみ。** 実データ投入は最終段まで行わない（次期送り）
- 依存ライブラリは CDN（cdnjs / jsdelivr / unpkg）から読む。ビルドツールは入れない

---

## 全アプリ共通の完成条件（DoD）

1. 評価セット（P3は20件）で加重スコアが閾値超え、かつ足切り項目を全通過
2. `./scripts/check-separation.sh` が通る
3. ライト／ダーク両対応、レスポンシブ
4. telemetry を1回の利用につき1レコード出力でき、**集計スクリプトで削減時間が算出できる**（ADR-010）
5. 利用手引き（冒頭にデータ区分表）
6. 外部文書を扱うアプリは injection_suite を実施済み
7. 可搬性テスト（G5）合格 ※A09は免除
8. 起動時に core と org-profile の `schema_version` を検証し、不一致なら停止する

**telemetry を出力できないアプリは完成扱いにしない。**
後から遡って取得できない唯一のデータであり、外販根拠はこれしかない。
特に `actual.manual_fix_minutes`（出力後の手直し時間）は必ず取る。
計上しないと削減率が2倍近く過大に出る。

---

## 着手順

祝日6日を反映した稼働実額（174h）で組み直したもの。**日付とゲート判定日つきの完全版は `docs/plan.md` §3。**

| 週 | 稼働 | 対象 |
|---|---|---|
| W1 | 12h | 基盤① telemetry 記録／エクスポート／集計スクリプト |
| W2 | 15h | 基盤② org-profile ローダ＋schema検証／共通シェル／採点ランナー |
| W3-5 | 36h | **A15 ワークプラン／工数見積キット** ← ここから。型キットはA15から抽出する |
| W6 | 15h | A03 データ投入可否ゲート ／ A15 実運用開始 |
| W7 | 12h | A04 AI利用ポリシー・チェッカー |
| W8-9 | 30h | A11 RFP／提案要件アナライザー |
| W10-11 | 27h | A09 提案書ストラクチャ・ジェネレーター |
| W12 | 15h | 予備 ／ A13（条件付き） ／ A03・A04 を配布して G2 着手 |
| W13 | 12h | telemetry 集計・四半期レビュー・次期計画・IP帰属の文書化 |

**W4（09/21-27）は祝日3日で稼働6h。** 集中作業を置かず、評価セット作成と Before 実測を寄せる。
要件定義は `docs/requirements.md`。

---

## 作業する上での約束

- **P1が埋まっていないアプリに着手しない。** 特に「現状値（Before）」が空のまま始めない。推定値で埋めると削減率が願望になる。
  取り方は `docs/baseline-measurement.md`。**`source: "assumption"` のまま P4 に入らない**
- **新しいアプリを思いついても、まず `docs/requirements.md` の見送りリストを見る。** 一度判断したものを蒸し返さない
- **評価軸の判定方法は `deterministic` を最優先。** LLMに採点させるのは、機械判定が原理的に不可能な場合のみ
- **一度決めた設計判断を蒸し返さない。** `docs/decisions.md` を見る。覆すときは新しい ADR を追加する（上書き削除しない）
- **未解決事項には期限がある。** `docs/plan.md` §7。期限を過ぎたら決まっていない前提で先に進む。待たない
- スコープを広げる提案より、**削る提案**を歓迎する。稼働は174hしかない
- **超過が2週続いたら本数を削る。** 機能を削るのではなく本数を削る。費用が上限に触れたら `effort` を下げるか `MODEL.ALT` に落とす（本数を削るより先にここ）
